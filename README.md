# Persistent Permalinks

BookStack theme container for the `persistent-permalinks` module.

Activate this theme with:

```env
APP_THEME=persistent-permalinks
```

The actual implementation lives in:

`themes/persistent-permalinks/modules/persistent-permalinks`
